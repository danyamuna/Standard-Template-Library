## C++ Stack
A stack stores multiple elements in a specific order, called LIFO.
LIFO stands for Last in, First Out.
// Include the stack library
#include <stack>
***Note: You cannot add elements to the stack at the time of declaration, like you can with vectors
stack<string> food= {"banana","apple}; //I made a mistake
stack.push("pizza");
stack.push("banana");
stack.pop(); // remove an element from the top of the stack
stack.empty();
stack..size():
stack.emplace();
stack.top(); //function is used to reference the top(or the newest) element of the stack.
